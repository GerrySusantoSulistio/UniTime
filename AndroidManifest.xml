<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31"
        android:name=".UniTime">
        <activity
            android:name=".LoadPageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Sign.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".Sign.SignUpActivity"
            android:exported="true" />
        <activity
            android:name=".MainMenu.MainMenuActivity"
            android:exported="true" />
        <activity
            android:name=".MainMenu.TimerActivity"
            android:exported="true" />
        <activity
            android:name=".MainMenu.BreakActivity"
            android:exported="true" />
        <activity
            android:name=".Achievement.AchievementActivity"
            android:exported="true" />
        <activity android:name=".Shop.ShopActivity" />
        <activity android:name=".introduction.IntroActivity" />
        <activity android:name=".ChooseCharacterActivity" />
        <activity android:name=".SettingsActivity"/>
        <activity android:name=".ChangePasswordActivity"/>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>